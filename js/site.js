/* ============================================================
 * 梦眠馆 · 古明地华 —— 站点配置
 *
 * 留言板使用 GitHub Issues 当云端存储（和熙叆站同一套方案）：
 *   · 读取：走 GitHub 公开接口，任何访客都不用登录就能看到全部留言
 *   · 发表：跳转到 GitHub 的发表页，用 GitHub 账号发布
 *   · 留言 = 标题以 prefix 开头的 issue；想换一块全新留言板就改 prefix
 *   · GitHub 未登录接口限 60 次/小时/IP，所以站内做了本地缓存
 *   · 预设留言的云端初始化脚本：tools/seed-board.js
 * ============================================================ */
window.HANA_SITE = {
  board: {
    owner: 'bannianhua',
    repo: 'komeiji-hana',
    prefix: '[留言板]',
    cacheMinutes: 5,   // 本地缓存分钟数（省额度、离线也能看）
    pageSize: 30,      // 每次显示多少条
    maxLen: 240        // 单条留言最大字数
  }
};
